# 更新日志 (CHANGELOG)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。Windows GUI/CLI 主线（main 分支）与 OpenWrt 插件（openwrt 分支）分别迭代。

## [1.2.0]

### 新增
- **门户地址自动发现 + 手动填写**（为适配其他同样用 web 门户的学校）
  - 识别不再认死门户 IP：`302` 的目标主机与请求主机不同即判为劫持（同主机跳转如 baidu→https 仍判"无结论"），
    相对路径按当前 URL 补全；登录页请求支持跟随跳转，POST 打到最终 URL
  - 探测不到时：先用数据文件里保存的门户地址，都没有则提示手动获取（浏览器打开任意 http 网站 → 复制跳转后的地址）
  - 界面新增「门户地址」输入框 +「检测门户」按钮；CLI 新增 `--portal URL`、`--detect-portal`、`--save-portal`
  - 自动发现成功时把门户基地址（`scheme://host`，不含会过期的会话参数）写回 `campus-auth.dat`
- **账号/密码字段名按登录页推断**：老板牌是 `userId`/`passwd`，别家门户可能叫 `account`/`pwd`/`username`；
  按 `type=password` 与其前一个可见文本输入判定，推不出来才回退固定名
- 探测地址可用环境变量 `CAMPUS_AUTH_PROBE` 覆盖（逗号分隔 `host[:port]/path`），便于测试或自定义

### 说明
- 已用"本地假门户"端到端验证换学校场景（跨主机劫持 + 不同字段名 account/pwd + 完整登录），
  并在真实校园网用测试账号跑通回归（字段名仍识别为 `userId`/`passwd`，认证成功）

## [1.1.0] - 2026-09-17

### 新增
- **一键下线本设备**：走门户自助管理接口（`/self` JSON API）实现程序化下线
  - GUI「下线本设备」按钮：确认 → 弹验证码小窗（GDI+ 解码内嵌显示，失败自动回退系统看图程序）→ 输入 4 位码 → 自动匹配本机会话 → 按 MAC 下线并**清除绑定** → 3 秒复核确认断网
  - CLI `--offline`：交互式弹图输码；`--list-online` 查看本账号在线设备；`--offline-code/--offline-session` 两段式供脚本自动化
  - 新模块 `src/selfsvc.rs`：SPA JSON 客户端引擎 + 设备行解析 + MAC 归一化匹配 + GDI+ 验证码解码
- **协议文档**：`docs/PROTOCOL.md` 新增 §7「自助管理下线接口」全文（含行 schema、调用规约、绑定粘性规则）

### 修复
- **日志区字体颜色**：两条渲染路径（未聚焦 `WM_CTLCOLORSTATIC` / 聚焦后 `WM_CTLCOLOREDIT`）统一为纯黑 + 不透明白底，修复初次绘制时 ClearType 在透明背景下的彩色毛边、点击后才变清晰的不一致
- **设备槽位误判**：查明「MAC 绑定粘性」——选电脑端却占手机槽的根因是旧绑定残留；下线流程默认清除绑定，下次认证按所选设备类型重新分类（实测修复本机槽位）
- **崩溃与文案**：`campus-auth.dat` 改为原子写入（临时文件+rename），避免写一半崩溃损坏加密凭据文件；「自助管理」按钮名与冲突提示文案对齐

### 重构
- 清除 `Box::leak` 泄漏式 UA 构造（`Auth.ua` 改为所有权 `String`）
- 全部编译警告清零（无用常量/导入/多余 `unsafe`/跨 bin 死代码带注释 allow）
- CLI 与 GUI 下线逻辑共用同一引擎，行为一致；下线/查询完成后自动丢弃自助会话

### 版本与构建
- 版本 1.0.0 → 1.1.0；新增 windows-sys 特性 `Win32_Graphics_GdiPlus`
- 构建 0 警告，6 项单元测试通过（GDI+ 解码链路 / DPAPI 往返 / MAC 匹配 / UCI 解析等）
- 实机端到端验证：PC 槽登录 → 验证码 → ByMac 下线 → 4 秒内回到未认证 → 重登后计费行分类为 PC(Windows)

## [1.0.0] - 2026-09-16

- 首个正式版：贺州学院校园网（bossWeb 门户）自动认证工具
- Win32 原生 GUI + CLI，单文件可执行、零运行时依赖
- 核心功能：一键 Web 认证（探测→会话 Cookie→B 组表单→复核放行）、双设备槽位（电脑/手机 UA）、网卡选择与源地址绑定、DPAPI 加密保存凭据、内嵌滚动日志、槽位冲突保护（绝不顶号）、幂等保活